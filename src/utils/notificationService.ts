export const sendNotification = async (message: string) => {
    // Stub for sending email or SMS
    console.log(`[NOTIFICATION SERVICE]: ${message}`);
    // In real app, integration with SendGrid or Twilio would go here.
};
